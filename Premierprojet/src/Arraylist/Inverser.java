package Arraylist;

import java.util.ArrayList;
/*
public class Inverser {

   // void inverser(){


  //  }

    public static void inverserTableau(int[] tab) {
        int n = tab.length;
        for (int i = 0; i < n / 2; i++) {
            // 1. On stocke temporairement la valeur de gauche
            int temp = tab[i];

            // 2. On met la valeur de droite à gauche
            tab[i] = tab[n - 1 - i];

            // 3. On met la valeur stockée (l'ancienne gauche) à droite
            tab[n - 1 - i] = temp;
        }
    }
    public static void main(String[] args) {
        ArrayList<String> elements= new ArrayList<>();

        //Ajouter elements
        elements.add("elem1");
        elements.add("elem2");
        elements.add("elem3");
        elements.add("elem4");


        //inverse


        }

        //System.out.println("valeurs" + elements.size());

    }

}
*/